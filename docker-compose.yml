services:
  task-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: task-api-container
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
